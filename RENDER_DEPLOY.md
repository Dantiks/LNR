# 🚀 Деплой на Render.com

## Автоматический деплой

1. **Перейдите на [Render Dashboard](https://dashboard.render.com/)**

2. **Создайте новый Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий: `Dantiks/LNR`
   - Render автоматически определит `render.yaml` конфигурацию

3. **Настройте переменные окружения**
   - В настройках сервиса добавьте:
     - `GROQ_API_KEY` = ваш API ключ от Groq
   - Или используйте `.env` для локальной разработки

4. **Деплой**
   - Нажмите "Create Web Service"
   - Render автоматически задеплоит приложение
   - После деплоя вы получите URL вида: `https://lnr-ai-chat.onrender.com`

## Ручной деплой через CLI

```bash
# Установите Render CLI (если еще не установлен)
npm install -g render

# Войдите в аккаунт
render login

# Деплой
render deploy
```

## Важные примечания

⚠️ **Free tier Render**:
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос может занять 30-50 секунд (cold start)
- 750 часов в месяц бесплатно

✅ **Автоматический редеплой**:
- При каждом `git push` в ветку `main` Render автоматически обновит приложение

🔧 **WebSockets**:
- Socket.io полностью поддерживается на Render
- Не требуется дополнительная настройка

## Проверка после деплоя

1. Откройте URL вашего приложения
2. Создайте новый чат
3. Отправьте тестовое сообщение
4. Убедитесь, что AI отвечает полными предложениями
